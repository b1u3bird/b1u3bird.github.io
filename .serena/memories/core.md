# Core

- `D:\Code\website` currently has no app scaffold; it is suitable for standalone static website files unless a future task introduces a framework.
- Existing checked-in content is `.spec-workflow/` templates plus Serena metadata, not an application.
- Workspace-level guidance from `D:\Code\CLAUDE.md` applies: treat `D:\Code` as a personal workspace of independent projects/files, not a repo-wide application.
- Related memories: tech/tooling in `mem:tech_stack`; runnable commands in `mem:suggested_commands`; style conventions in `mem:conventions`; completion checks in `mem:task_completion`.