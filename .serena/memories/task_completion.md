# Task Completion

- No project-specific lint/test/build command exists.
- For static website edits, validate by opening the HTML directly or serving with `python -m http.server 8000` and checking layout manually/browser-side.
- If JavaScript is added, check browser console when possible.
- Mention when browser automation cannot run due missing local browser/runtime.