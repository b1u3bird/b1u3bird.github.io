# Conventions

- Prefer simple static files for this project until a real scaffold is introduced.
- Keep edits scoped to the target page/site; avoid adding package metadata or framework files unless requested.
- User global preference: respond in Simplified Chinese; for C++/Python tasks follow the coding style in global CLAUDE.md, but this project currently has no such code.
- For frontend work, maintain responsive layout and avoid decorative-only UI that does not support the requested experience.